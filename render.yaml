services:
  - type: web
    name: liang-safety-system
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: liang-safety-db
          property: connectionString

databases:
  - name: liang-safety-db
    plan: free
    databaseName: liang_safety
    user: liang_safety_user